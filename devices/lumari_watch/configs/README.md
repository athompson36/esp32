# Lumari Watch — Configs

Build and runtime config presets for Lumari Watch.

- Add example env files or config snippets here (e.g. `lumari_watch_example.env`) once the firmware repo defines build flags or runtime options.
- See [firmware/lumari_watch/README.md](../firmware/lumari_watch/README.md) for clone and build.
